{
  "version": 2,
  "buildCommand": "pnpm --filter @workspace/mindcare run build:vercel && rm -rf dist/public && mkdir -p dist && cp -a artifacts/mindcare/dist/public dist/public",
  "outputDirectory": "dist/public",
  "functions": {
    "api/index.ts": {
      "runtime": "nodejs22.x",
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}